function tratarEntrada(entrada){
    if(entrada === 'Pe'|| entrada === 'pe'|| entrada === 'Ped'|| entrada === 'ped'|| 
    entrada === 'Pedr'|| entrada === 'pedr'|| entrada === 'pedra' || entrada === 'Pedra'|| 
    entrada === 'peda'|| entrada === 'Peda'|| entrada === 'rock'){
        entrada = 'pedra';
    }
    if(entrada === 'Pe'|| entrada === 'pe'|| entrada === 'Ped'|| entrada === 'ped'|| 
    entrada === 'Pedr'|| entrada === 'pedr'|| entrada === 'pedra' || entrada === 'Pedra'|| 
    entrada === 'peda'|| entrada === 'Peda'|| entrada === 'rock'){
        entrada = 'pedra';
    }
}